import { PositionRefDirective } from './position-ref.directive';

describe('PositionRefDirective', () => {
  it('should create an instance', () => {
    const directive = new PositionRefDirective();
    expect(directive).toBeTruthy();
  });
});
